language: python
python:
  - 2.7
install:
  - pip install -e .
  - pip install coveralls
  - pip install --upgrade setuptools
script:
  - coverage run --source=faker setup.py test
after_success:
  - coveralls
